# Сайт БТИ Самара — ИП Баймурзин А. Р.

Лендинг кадастрового инженера. **К проекту Fortune VPN отношения не имеет** — это
отдельный сторонний проект, свой репозиторий, свои деплои.

## Первое, что нужно сделать в новой сессии

В папке лежит **незапушенный коммит** с правками заказчика. Выложить:

```
git push origin main
railway up --ci
```

Обе команды из корня этой папки. Первая обновляет основную ссылку, вторая — резервную.
Сборка GitHub Pages идёт ~40 секунд, Railway ~1 минуту.

Раньше деплой блокировал защитный хук репозитория Fortune VPN — он ловил
`git push main` и `railway up` по тексту команды. Из этой папки хук не действует.

## Куда деплоится

| Ссылка | Что это |
|---|---|
| https://downmeansoff.github.io/bti-samara/ | **Основная**, её раздаём клиентам. GitHub Pages, repo `downmeansoff/bti-samara`, ветка `main`. Бесплатно бессрочно |
| https://bti-samara-landing-production.up.railway.app | Резервная. Railway, проект `bti-samara-landing` |

⚠️ `*.up.railway.app` у части российских провайдеров режет РКН — посетитель видит
«не открывается». Поэтому основной раздаём github.io.

Идеал на будущее — свой домен (200–1500 ₽/год). Владелец пока не купил.

## Как устроен сайт

Client-rendered на **dc-runtime**: `support.js` разворачивает `<x-dc>`, `<dc-import>`,
`sc-for`, `sc-if`. Компоненты — `*.dc.html`, логика в `class Component extends DCLogic`.
Картинки-слоты — `image-slot.js` + `.image-slots.state.json` (dotfile с base64).

Раздаётся `server.js` — Node без зависимостей: слушает `$PORT`, `/` редиректит на
`index.dc.html`, HTML/JS отдаёт с `no-cache`, картинки на сутки.

Локально посмотреть:
```
PORT=3999 node server.js
```

**Мобильная вёрстка живёт целиком в `Header.dc.html`**, в блоке `<style>` внутри `helmet`.
Header грузится на каждой странице, поэтому правила действуют везде. Вся вёрстка сайта —
инline-стили, поэтому селекторы цепляются к ним: `[style*="padding: 88px 24px"]`.
⚠️ React сериализует стиль **с пробелом после двоеточия** и переводит `0` в `0px` —
селектор `[style*="padding:88px"]` не сработает.

**Внешних зависимостей нет и добавлять нельзя** — аудитория российская. React и шрифты
лежат свои: `assets/vendor/react*.min.js`, `assets/fonts/`. Раньше тянулись с unpkg.com
и Google Fonts; при блокировке unpkg страница была бы полностью пустой.
Проверка: открыть сайт и убедиться, что внешних сетевых запросов ноль.

## Правила по контенту

- **Делать строго то, что прислал владелец.** Без самодеятельности в текстах, цифрах
  и блоках. Технические улучшения (превью ссылки, фавикон, мобильная панель) он не
  оспаривал, но содержимое — только по его списку.
- **Персональные данные замазывать.** В сканах документов были СНИЛС и дата рождения —
  закрыты чёрными плашками. Оригинальные PDF выписки СРО не публиковать.
- Правки в `.dc.html` — обычный inline-HTML, пересборка не нужна.

## Что сейчас на сайте

Услуги (3): межевание · технический план объекта недвижимости · перераспределение,
раздел и объединение участков.

Цифры: **12+ лет**, **1000+ объектов** на кадастровом учёте. Ровно две плашки —
третью владелец просил не добавлять.

Аттестат № 02-13-942, член СРО «Содружество» рег. № 704, проверка ведёт на
https://www.ski-pk.ru/sro/members/41ef022f-7d44-42b1-9f89-ad0900971a41

Форма заявки собирает имя/телефон/услугу и открывает Telegram `@bti_samara63` с готовым
текстом — клиент жмёт «отправить». Работает, платить за это не нужно. Есть галочка
согласия на обработку данных, без неё заявка не уходит; политика — `politika.dc.html`.

Блок отзывов снят: те, что были, придумал конструктор сайта. Вернуть, когда владелец
даст настоящие.

## Открытые вопросы к владельцу

- Почта `info@bti-samara.ru` **не его** — домен чужой, письма уходят в никуда.
  Нужен рабочий адрес либо убрать почту с сайта.
- Нужны реальные фотографии — сейчас все стоковые.
- Нужны цены ещё по трём услугам, сейчас «по запросу».
- Нет ни одного примера работ, хотя объектов больше тысячи.
- Реквизиты ИП (ОГРНИП, ИНН) не опубликованы — ждёт его разрешения.

Подробный разбор с приоритетами: https://claude.ai/code/artifact/2c0ff8d1-02f9-41a7-8fd5-e746586d479a
